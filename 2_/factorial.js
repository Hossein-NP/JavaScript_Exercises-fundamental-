"use strict";

function factorial_calculator(n){

  if(n == 0 || n == 1){
    return 1;
  }
  else {
    return 
  }
}

let number = +prompt("لطفا عدد مورد نظر برای محاسبه فاکتوریل وارد کنید:" , "");
let result = factorial_calculator(number);
console.log(`factorial result : ${result}`);